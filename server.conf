http {
    port: 14880;
    web_root: ./www/;

    route {
        mapping: /;
        file_path: index.html;
    }

    route {
        web_root: ./www/subfolder/;
        mapping: 200;
        file_path: 200.html;
    }
}
